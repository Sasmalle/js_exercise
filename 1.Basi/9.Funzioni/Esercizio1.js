/*Scrivi una funzione che prenda due parametri, base e altezza, 
e restituisca l'area del rettangolo corrispondente.*/

function calocoloAreaRettangolo(base, altezza){
    return base*altezza
}
let base = 20;
let altezza = 30;
const areaRettangolo = calocoloAreaRettangolo(base, altezza);

console.log(areaRettangolo);
